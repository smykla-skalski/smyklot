dist
node_modules
package-lock.json
.svelte-kit

# Written by the dev mock server as the panel is used, so its shape is whatever
# was stored last. Prettier does not read .gitignore, so without this the lint
# fails for anyone who has run the mock.
dev/.mock-preferences.json
