# Generated panel assets

`bundle.zip` is built by `mise run panel:assets:generate` and intentionally ignored by Git.

The directory remains in the repository so Go tooling can inspect the embedding package before the archive is generated.
