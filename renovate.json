{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "automergeStrategy": "squash",
  "extends": [
    "config:best-practices",
    ":semanticCommits",
    ":gitSignOff",
    ":dependencyDashboard",
    ":label(dependencies)",
    ":rebaseStalePrs",
    ":enableVulnerabilityAlertsWithAdditionalLabel(kind/security)",
    ":automergeMinor",
    ":automergeDigest",
    ":automergeLinters",
    ":automergeTesters",
    ":prConcurrentLimit20",
    "helpers:pinGitHubActionDigests",
    "security:openssf-scorecard",
    "group:linters"
  ],
  "gitIgnoredAuthors": ["205507218+smyklot[bot]@users.noreply.github.com"],
  "ignorePaths": [
    "**/tmp/**",
    ".github/workflows/smyklot-pr-commands.yml",
    ".github/workflows/smyklot-poll.yml"
  ],
  "osvVulnerabilityAlerts": true,
  "packageRules": [
    {
      "addLabels": ["area/ci"],
      "commitMessageTopic": "{{depName}} action",
      "description": "GitHub Actions dependencies",
      "matchManagers": ["github-actions"],
      "semanticCommitType": "ci"
    },
    {
      "description": "Group Go tool dependencies",
      "groupName": "Go tooling",
      "matchDepTypes": ["require"],
      "matchManagers": ["gomod"],
      "matchPackageNames": ["/^golang.org/x/tools/"]
    },
    {
      "description": [
        " Group dependency bumps from selected GitHub sources into a single PR across managers",
        " (e.g., mise and gomod) to avoid separate PRs per manager"
      ],
      "groupName": "{{{replace 'https://github\\.com/[a-z-]+/' '' sourceUrl}}}",
      "matchSourceUrls": [
        "https://github.com/{onsi/ginkgo,golangci/golangci-lint}"
      ]
    },
    {
      "description": "Group Go version updates from gomod and mise into a single PR",
      "groupName": "go version",
      "matchDepNames": ["go"],
      "matchManagers": ["gomod", "mise"],
      "rangeStrategy": "bump"
    },
    {
      "allowedVersions": "<1.0.0",
      "description": [
        " The cookie override reaches the CVE-2024-47764 patch inside the line SvelteKit 2 declares,",
        " so it is a decision about that release rather than a dependency to keep current. 2.x removed",
        " parse and serialize, the two functions Kit imports, and the build dies on them; 1.x keeps",
        " them only as untyped aliases that 2.x then deleted. Lift this when the panel moves to",
        " SvelteKit 3, which asks for cookie 2 itself"
      ],
      "matchManagers": ["npm"],
      "matchPackageNames": ["cookie"]
    },
    {
      "description": "Smyklot action managed by sync-smyklot workflow",
      "enabled": false,
      "matchManagers": ["github-actions"],
      "matchPackageNames": ["smykla-skalski/smyklot"]
    }
  ],
  "rebaseWhen": "conflicted",
  "vulnerabilityAlerts": {
    "automerge": true
  }
}
